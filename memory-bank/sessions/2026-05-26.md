# Session: 2026-05-26

**Started**: 2026-05-26 01:30 IST
**Focus Task**: T4
**Status**: 🔄 ACTIVE

## Work Done

### Removed Broken LaunchAgent
- Removed `com.deepak.mac-process-monitor.dashboard.plist` from `~/Library/LaunchAgents/`
- It had crashed 4,427 times due to wrong WorkingDirectory (pointed at `~` instead of project root)
- Dashboard now started manually via `npx tsx src/combined.ts`

### DB Size Indicator
- Added `/api/db-size` endpoint to `src/web/server.ts` and `src/combined.ts`
- Returns `size`, `sizeMB`, `path` of the SQLite DB
- Frontend: `loadDbSize()` fetches every 5s, displays `🗄 15.91 MB` badge top-right

### Uptime Badge
- Added `/api/server-info` endpoint to `src/web/server.ts` and `src/combined.ts`
- Computes human-readable uptime string from `serverStartTime = Date.now()`
- Frontend: `loadServerInfo()` displays `⏱ 3h 42m 15s` badge top-right

### Fixed Battery Property Casing
- `src/core/SystemCollector.ts`: Fixed `systeminformation` camelCase fields:
  - `battery.ischarging` → `battery.isCharging`
  - `battery.acconnected` → `battery.acConnected`
  - `battery.timeremaining` → `battery.timeRemaining`
  - `battery.cyclecount` → `battery.cycleCount`
- Added "Plugged In" state: when `isPlugged=true` but `isCharging=false`
- Time remaining shows "AC power" when plugged in

### Fixed Profile Filtering
- Filter persists across refreshes
- "Show All" clears filter correctly
- No null warnings in console
- Sorting works within active filter
- Empty filter shows "No matching processes currently running"

### Moved Screenshots
- All Playwright PNG screenshots moved to `.playwright-mcp/`
- Added `.playwright-mcp/` to `.gitignore`
- Deleted `dashboard-screenshot.png` from repo root

### Created `src/combined.ts` — Unified Monitor + Dashboard
- New file: embeds `Monitor` loop + HTTP server in a single process
- Shares `TimeSeriesDB` and `SystemCollector` instances
- All API endpoints: `/api/snapshot`, `/api/history`, `/api/drain-events`, `/api/process-history`, `/api/process-stats`, `/api/top-processes`, `/api/profiles`, `/api/db-size`, `/api/server-info`
- Serves static files from `web/public/`
- Graceful shutdown on `SIGINT`/`SIGTERM`
- **Current command**: `npx tsx src/combined.ts`

### Chart Tabs — Battery / CPU / Memory
- Replaced single "Battery History" panel with tabbed chart interface
- Tabs: Battery (cyan), CPU (amber), Memory (purple)
- `switchChartTab()` changes active metric
- `renderLineChart()` renders SVG line chart with gradient fill for any metric
- Sampled data points (~15 dots max) with hover tooltips

### Current State
- `npx tsx src/combined.ts` runs everything
- DB actively recording: 2,976 snapshots, 148,800 process samples
- Server on PID 51470, port 3456
- All chart tabs (Battery/CPU/Memory) plotting live data

## Files Modified

- `src/combined.ts` — Created (unified monitor + dashboard process)
- `src/web/server.ts` — Added `/api/db-size`, `/api/server-info` endpoints
- `src/core/SystemCollector.ts` — Fixed battery property casing
- `web/public/app.js` — Chart tabs, DB size, uptime, profile filtering, SVG line chart
- `web/public/index.html` — Header badges, chart tabs, SVG chart structure
- `web/public/styles.css` — Badge styles, chart tab styles, SVG chart styles
- `.gitignore` — Added `.playwright-mcp/`
- `memory-bank/database/import-existing-data.js` — Fixed paths for `memory-bank/` subdir

## Next Steps

- T2: Telegram/OpenClaw Alert Integration (HIGH priority)
- T3: Per-Process History Query Interface (MEDIUM priority)
- T5: Swift Menubar App (LOW priority — future)
