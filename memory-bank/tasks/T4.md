# T4: Web Dashboard for Live Monitoring

*Created: 2026-05-18*
*Last Updated: 2026-05-26 13:19 IST*

**Status**: 🔄 **IN PROGRESS**
**Priority**: MEDIUM

## Details

### Completed Work

**2026-05-18 — Initial Dashboard**
- Native Node HTTP server (`src/web/server.ts`) with live data endpoints
- Dark theme UI with KPI cards (battery, CPU, memory, status)
- Sortable process table with CPU/memory bars
- Battery history bar chart

**2026-05-19 — Expanded Metrics**
- 15 new snapshot columns (CPU breakdown, memory MB, swap, load, disk I/O, network, disk usage, temperature)
- Auto-migration via ALTER TABLE for schema upgrades
- WAL mode for concurrent reads

**2026-05-25 — UI Polish**
- Mobile-responsive process table with horizontal scroll
- Clickable column headers for sorting (asc/desc toggle)
- SVG line chart for battery history with gradient fill
- Process memory displayed in actual MB (not percentage)
- Chart axes (Y-axis %, X-axis time labels)

**2026-05-26 — Major Enhancements**
- **`src/combined.ts`**: Unified monitor + dashboard in single process
- **Chart tabs**: Battery / CPU / Memory — all plot live SVG line charts
- **DB size badge**: `🗄 15.91 MB` indicator top-right
- **Uptime badge**: `⏱ live server uptime` indicator top-right
- **Profile filtering**: Filter persists, "Show All" works, sorting within filter
- **Battery status fixes**: "Plugged In" state, AC power display, property casing fix
- **Screenshot cleanup**: All PNGs moved to `.playwright-mcp/`

### API Endpoints

| Endpoint | Description |
|----------|-------------|
| `GET /api/snapshot` | Live system snapshot (battery, CPU, processes) |
| `GET /api/history?minutes=N` | Time-series history from DB |
| `GET /api/drain-events` | Detected drain events |
| `GET /api/process-history?name=X&minutes=N` | Per-process CPU/memory history |
| `GET /api/process-stats?name=X&minutes=N` | Process statistics + PID history |
| `GET /api/top-processes?metric=cpu&limit=N` | Top N processes by metric |
| `GET /api/profiles` | Process profile management |
| `GET /api/db-size` | SQLite DB size in MB |
| `GET /api/server-info` | Server uptime, port, host |

### Known Limitations

1. **No auth**: Dashboard open on local network (acceptable for home use)
2. **No WebSocket**: Polling every 5s (simple but not real-time)
3. **Sampling**: Chart samples to ~15 points max for visibility

### Files

| File | Purpose |
|------|---------|
| `src/combined.ts` | Unified monitor + dashboard process |
| `src/web/server.ts` | Standalone dashboard HTTP server |
| `web/public/index.html` | Dashboard markup |
| `web/public/app.js` | Frontend logic |
| `web/public/styles.css` | Dark theme styling |
